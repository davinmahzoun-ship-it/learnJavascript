1. Run this command to create a new npm project: `npm init -y` (We need an npm project to download libraries from npmjs.com)

2. Above command creates a `package.json` file that describes the project.

3. Run `npm i @supabase/supabase-js` to download Supabase.

4. We never touch `package-lock.json`.